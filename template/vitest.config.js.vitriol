import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    setupFiles: ['/test/setup.js']
  },
  // if isJSX
  esbuild: {
    jsxFactory: 'm',
    jsxFragment: "'['"
  },
  // end isJSX 
})